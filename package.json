{
  "name": "gobol",
  "description": "GOBOL Language Server extention for vscode",
  "author": "João Machado",
  "publisher": "popandboom",
  "license": "MIT",
  "version": "1.0.0",
  "categories": [],
  "engines": {
    "vscode": "^1.75.0"
  },
  "activationEvents": [ "onLanguage:cobol" ],
  "main": "./client/out/extension.js",
  "files": [
    "client/out",
    "server/GOBOL-LSP"
  ],
  "contributes": {
    "languages": [
      {
        "id": "cobol",
        "aliases": ["COBOL", "cobol"],
        "extensions": [".cbl", ".cob", ".pcod", ".PCOD"]
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./client/tsconfig.json",
    "watch": "tsc -p ./client/tsconfig.json -w",
    "postinstall": "cd client && npm install && cd .. && npm run install_server",
    "clone_server": "rm -rf server && git clone https://github.com/jtomaspm/GOBOL-LSP server && rm -rf server/.git",
    "install_server": "npm run clone_server && cd server && go build -o GOBOL-LSP ./cmd/lsp/main.go && cd .."
  },
  "devDependencies": {
    "@types/node": "^20.5.7",
    "typescript": "^5.2.2"
  }
}
