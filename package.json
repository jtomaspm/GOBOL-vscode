{
  "name": "gobol",
  "description": "GOBOL Language Server extention for vscode",
  "author": "João Machado",
  "publisher": "popandboom",
  "license": "MIT",
  "version": "1.0.0",
  "categories": [],
  "engines": {
    "vscode": "^1.75.0"
  },
  "activationEvents": [ "onLanguage:cobol" ],
  "main": "./client/out/extension.js",
  "files": [
    "client/out",
    "server/GOBOL-LSP"
  ],
  "contributes": {
    "languages": [
      {
        "id": "cobol",
        "aliases": ["COBOL", "cobol"],
        "extensions": [".cbl", ".cob", ".pcod", ".PCOD"]
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./client/tsconfig.json",
    "watch": "tsc -p ./client/tsconfig.json -w",
    "postinstall": "cd client && npm install && cd .. && npm run setup_server",
    "clone_server": "rm -rf server && git clone https://github.com/jtomaspm/GOBOL-LSP server_build",
    "build_server": "cd server_build && go build -o GOBOL-LSP ./cmd/lsp/main.go && cd ..",
    "install_server" : "mkdir server && mv server_build/GOBOL-LSP server/GOBOL-LSP && rm -rf server_build && chmod +x server/GOBOL-LSP",
    "setup_server": "npm run clone_server && npm run build_server && npm run install_server"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.2.2"
  }
}
